// eslint.config.js
t/** @type {import('eslint').ESLint.ConfigData} */
module.exports = {
  root: true,
  extends: [],    // no hereda ninguna configuración
  rules: {},      // sin reglas activas
  ignorePatterns: ['**/*']  // ignora todos los archivos
};